/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run atlas -profile test
 */

params {
	config_profile_name = 'Test MS profile'
  	config_profile_description = 'Minimal test dataset to check pipeline function for MS apps'
  	// Limit resources so that this can run on GitHub Actions
  	max_cpus = 2
  	max_memory = 10.GB
  	max_time = 48.h

	//Input data: 
	rawfile   = "/users/pr/qsample/files_processed/2021MS002_SUVA_004_02_2ug.raw.SP_Human"
        var_modif = "'Oxidation (M)' 'Acetyl (N-term)'"
}
